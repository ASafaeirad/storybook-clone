import { Space, Stack, Text } from '@mantine/core';

export const LogoutConfirm = () => {
  return (
    <Stack spacing={40}>
      <Text color="fgDefault">آیا میخواهید خارج شوید؟</Text>
      <Space h={0} />
    </Stack>
  );
};
